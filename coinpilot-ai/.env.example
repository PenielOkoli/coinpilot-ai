# Server-side only. Never prefix these with NEXT_PUBLIC_ — that exposes them to the client bundle.
# See docs/risk-register.md (R1) for why these must stay server-side.

ANTHROPIC_API_KEY=
MARKET_DATA_API_KEY=
EXCHANGE_API_KEY=
EXCHANGE_API_SECRET=

# Session/store — fill in once a database is chosen in a later module
DATABASE_URL=
